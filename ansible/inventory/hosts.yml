all:
  children:
    webservers:
      hosts:
        web1:
          ansible_host: 20.74.82.20
      vars:
        ansible_user: azureuser
        ansible_ssh_private_key_file: ~/.ssh/id_rsa
        ansible_python_interpreter: /usr/bin/python3
